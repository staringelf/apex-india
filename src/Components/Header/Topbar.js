import Address from "./Address";

const Topbar = () => {
  return (
    <section className="topbar">
      <div class="across-width">
        <Address />
      </div>
     </section>
  )
}

export default Topbar;